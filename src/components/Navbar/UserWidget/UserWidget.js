import React,{useRef, useEffect} from 'react'

import './UserWidget.css'
const UserWidget = ({showUserWidget, setShowUserWidget, currentUser}) => {
   

    return (
       <p>w</p>
    )
}

export default UserWidget
